--Variables--

local notLogin = true

--Code--

--Startup script--
term.setBackgroundColor(colors.white)
term.clear()

--Main Loop--

while notLogin do
	GUI.ResetButtons()
	GUI.DrawButton({
		1,
		1,
		5,
		6,
		colors.blue,
		2,
		2,
		"Hi",
		colors.black,
		"hi",
	})
end