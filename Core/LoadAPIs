--Code--
local apis = fs.list("TheOS/Core/API/")
for i,v in pairs(apis) do
	if fs.isDir("TheOS/Core/API/"..v) then
		apis[i] = nil
	end
end
for i,v in pairs(apis) do
	os.loadAPI("TheOS/Core/API/"..v)
end