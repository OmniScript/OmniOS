--[[
	System API
	for 
	TheOS
	by Creator
]]--

function reboot()
	os.reboot()
end

function newTask(name,title,...)
	local mainPath = "TheOS/Programs/"..name..".app/Main.lua"
	if fs.exists(mainPath) then
		local func, err = loadfile(mainPath)
		if func then
			newRoutine(name,title,func,...)
		end
	end
	return false
end