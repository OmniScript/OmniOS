local tArgs = {...}
local w,h = term.getSize()
local tReturnTable = {
	Button = {},
	Text = {
		Title = {
			name = "Title",
			text = "Select",
			xPos = 1,
			yPos = 1,
			bgColor = colors.gray,
			fgColor = colors.white,
		},
	}
}